<!DOCTYPE html>
<html lang="en">
    <head>
      <meta charset="utf-8" />
      <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
      <meta name="description" content="" />
      <meta name="author" content="" />
      <title>Ecommerceapp</title>
      <%= csrf_meta_tags %>
      <%= csp_meta_tag %>

      <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
      <%= stylesheet_link_tag 'site', media: 'all', 'data-turbolinks-track': 'reload' %>
      <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
      <%= javascript_include_tag 'site', 'data-turbolinks-track': 'reload' %>
      <%= javascript_include_tag 'notify' %>


      <!-- Favicon-->
      <link rel="icon" type="<%= image_path("favicon.ico") %>" rel="shortcut icon" />
    </head>
    <body>
      <%= render partial: "layouts/site_shared/navigation" %>
      <%= form_tag site_search_path, method: :get do %>
        <div class="row">
          <div class="well">
            <div class="input-group input-group-sm mb-3">
              <%= text_field_tag :query, params[:query], { class: 'form-control', placeholder: 'Pesquisar por...' } %>
              <div class="input-group-prepend">
                <span class="input-group-btn">
                  <%= submit_tag 'Pesquisar', class: 'btn btn-outline-secondary' %>
                </span>
              </div>
            </div>
          </div>
        </div>
      <% end %>
      <%= render partial: "layouts/site_shared/header" %>
      <%= yield %>
      <%= render partial: "layouts/site_shared/footer" %>
      <%= render partial: 'layouts/shared/notifications' %>
    </body>
</html>

